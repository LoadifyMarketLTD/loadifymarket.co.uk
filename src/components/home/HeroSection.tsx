import Button from '../ui/button';
import React from 'react';

const HeroSection = () => {
    return (
        <div>
            {/* Hero Section Content */}
        </div>
    );
};

export default HeroSection;