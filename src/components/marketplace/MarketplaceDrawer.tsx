import { useState, useEffect, useRef } from "react";
import { createPortal } from "react-dom";
import { Link } from "react-router-dom";
import { X, ChevronDown, ArrowRight } from "lucide-react";
import DrawerAccountBlock from "@/components/mobile/DrawerAccountBlock";
import DrawerCTACards from "@/components/mobile/DrawerCTACards";
import logo from "@/assets/LOGO.png";
import type { User } from "@/types";
import CATEGORY_CONFIG from "@/lib/category-config";
import { marketplaceSubcategorySlug } from "@/data/marketplaceTaxonomy";

interface Props { open:boolean; onClose:()=>void; user:User|null; dashboardPath:string; onLogout:()=>void; }

export default function MarketplaceDrawer({ open, onClose, user, dashboardPath, onLogout }: Props) {
  const [expandedSlug,setExpandedSlug]=useState<string|null>(null);
  const closeBtnRef=useRef<HTMLButtonElement>(null);
  const panelRef=useRef<HTMLDivElement>(null);

  useEffect(()=>{ if(!open){const t=setTimeout(()=>setExpandedSlug(null),300);return()=>clearTimeout(t);} const f=setTimeout(()=>closeBtnRef.current?.focus(),50); const onKey=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose();}; document.addEventListener('keydown',onKey); return()=>{clearTimeout(f);document.removeEventListener('keydown',onKey);};},[open,onClose]);
  useEffect(()=>{document.body.style.overflow=open?'hidden':'';return()=>{document.body.style.overflow='';};},[open]);

  return createPortal(<><div className={`fixed inset-0 z-[9998] bg-[#0A234F]/35 transition-opacity duration-300 ${open?'pointer-events-auto opacity-100':'pointer-events-none opacity-0'}`} onClick={onClose} aria-hidden="true"/><div ref={panelRef} className={`fixed left-0 top-0 z-[9999] flex h-[100dvh] w-[85vw] max-w-[380px] flex-col border-r border-[#0A234F]/10 bg-[#F8F7F4] shadow-2xl transition-transform duration-300 ${open?'translate-x-0':'-translate-x-full'}`} role="dialog" aria-modal="true" aria-label="Marketplace navigation" style={{paddingTop:'env(safe-area-inset-top, 0px)'}}><div className="flex h-full flex-col bg-[#F8F7F4] text-[#0A234F]"><div className="flex h-16 shrink-0 items-center justify-between border-b border-[#0A234F]/10 px-4"><Link to="/marketplace" onClick={onClose} className="flex min-w-0 items-center" aria-label="Loadify Marketplace — Home"><img src={logo} alt="Loadify Market" className="h-10 w-auto max-w-[190px] object-contain"/></Link><button ref={closeBtnRef} onClick={onClose} className="rounded-lg p-2 text-[#667085] hover:bg-[#0A234F]/5 hover:text-[#0A234F]" aria-label="Close menu"><X className="h-5 w-5"/></button></div><div className="flex-1 overflow-y-auto"><DrawerAccountBlock user={user} dashboardPath={dashboardPath} onLogout={onLogout} onClose={onClose}/><div className="mx-4 h-px bg-[#0A234F]/10"/><div className="pt-4"><p className="px-4 pb-2 text-[11px] font-bold uppercase tracking-widest text-[#667085]">Quick Actions</p><DrawerCTACards onClose={onClose}/></div><div className="mx-4 h-px bg-[#0A234F]/10"/><div className="flex items-center justify-between px-4 pb-2 pt-4"><p className="text-[11px] font-bold uppercase tracking-widest text-[#667085]">Browse Categories</p><Link to="/catalog" onClick={onClose} className="flex items-center gap-1 text-[11px] font-bold text-[#8A7351]">All <ArrowRight className="h-3 w-3"/></Link></div><nav aria-label="Marketplace categories">{CATEGORY_CONFIG.map(cat=>{const Icon=cat.icon;const isOpen=expandedSlug===cat.slug;const categoryUrl=`/category/${cat.slug}`;return <div key={cat.slug}><div className="flex border-b border-[#0A234F]/[0.07]"><Link to={categoryUrl} onClick={onClose} className="flex min-h-[50px] flex-1 items-center gap-3 px-4 hover:bg-[#0A234F]/[0.035]"><Icon className="h-[18px] w-[18px] shrink-0 text-[#8A7351]"/><span className="flex-1 text-left text-[15px] font-semibold">{cat.label}</span></Link>{cat.subcategories.length>0&&<button type="button" onClick={()=>setExpandedSlug(isOpen?null:cat.slug)} className="flex w-12 items-center justify-center text-[#8A94A3]" aria-expanded={isOpen}><ChevronDown className={`h-4 w-4 transition-transform ${isOpen?'rotate-180':''}`}/></button>}</div>{isOpen&&cat.subcategories.length>0&&<div className="border-b border-[#0A234F]/10 bg-white/60">{cat.subcategories.map(sub=><Link key={sub} to={`${categoryUrl}?sub=${encodeURIComponent(marketplaceSubcategorySlug(cat.label,sub))}`} onClick={onClose} className="flex h-[42px] items-center border-b border-[#0A234F]/[0.06] px-8 text-[14px] font-medium text-[#5A6578] last:border-b-0">{sub}</Link>)}</div>}</div>;})}</nav><Link to="/catalog" onClick={onClose} className="mx-4 mt-4 flex min-h-11 items-center justify-center gap-2 rounded-lg border border-[#0A234F]/10 bg-white px-4 text-sm font-semibold">Browse all categories <ArrowRight className="h-4 w-4 text-[#8A7351]"/></Link><div className="mx-4 mt-4 h-px bg-[#0A234F]/10"/><nav aria-label="Support links" className="flex flex-col py-2"><Link to="/register?type=seller" onClick={onClose} className="flex h-11 items-center px-4 text-sm font-medium text-[#5A6578]">Start Selling</Link><Link to="/shipping-policy" onClick={onClose} className="flex h-11 items-center px-4 text-sm font-medium text-[#5A6578]">Shipping Policy</Link><Link to="/wholesale-info" onClick={onClose} className="flex h-11 items-center px-4 text-sm font-medium text-[#5A6578]">Marketplace Information</Link><Link to="/" onClick={onClose} className="flex h-11 items-center px-4 text-sm font-medium text-[#5A6578]">About Loadify Platform</Link></nav></div></div></div></>,document.body);
}
