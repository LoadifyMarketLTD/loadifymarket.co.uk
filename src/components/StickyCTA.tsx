/**
 * StickyCTA — retired. All position:fixed CTAs have been removed.
 * The component is kept to avoid import errors on any legacy references.
 */
export default function StickyCTA() {
  return null;
}
