Known limitations in this agent environment

1) APK generation
- /artifacts/mobile-app-latest.apk was not generated in this run.
- Reason: Android/Capacitor signing + native build pipeline and full mobile runtime setup are not available in this sandbox by default.
- Existing hook: npm run build:android (android:build script in package.json).

2) Video recordings
- The following files were not generated in this run:
  - /artifacts/mobile-flow-home.mp4
  - /artifacts/mobile-flow-buyer.mp4
  - /artifacts/mobile-flow-seller.mp4
  - /artifacts/mobile-flow-admin.mp4
  - /artifacts/web-mobile-home.mp4
  - /artifacts/web-mobile-product.mp4
  - /artifacts/web-mobile-dashboard.mp4
- Reason: no integrated mobile/emulator video capture pipeline is configured in-repo for this sandbox session.

3) Deploy preview URL
- A web mobile preview URL is not produced from this local agent environment.
- Use repository CI/CD deploy previews (Netlify) to capture URL and mobile viewport recordings.
