<!-- Legacy foreground vector — superseded by the PNG bitmaps in mipmap-*/ic_launcher_foreground.png
     which are used by mipmap-anydpi-v26/ic_launcher.xml on Android 8+.
     Kept as a no-op transparent placeholder so builds targeting API < 26 still compile. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportHeight="108"
    android:viewportWidth="108">
    <!-- Transparent — actual icon comes from mipmap-*/ic_launcher_foreground.png -->
    <path
        android:fillColor="#00000000"
        android:pathData="M0,0h108v108h-108z" />
</vector>
